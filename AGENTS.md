# ベトナム旅行しおりアプリ 作業ルール

## 対象

- このルールは、このフォルダの「ベトナムのしおり」Webアプリに適用する。
- 回答は日本語で行い、専門用語はできるだけ避ける。必要な場合は短い意味を添える。
- 指示された範囲だけを最小限変更し、旅程・文章・デザイン・機能・データへ不要な変更を加えない。

## アプリ変更時の標準フロー

ユーザーが旅程、文章、リンク、デザイン、機能、不具合など、アプリへの変更を直接指示した場合は、その指示を公開中のアプリまで反映する指示として扱う。特別な指示がない限り、確認を求めず次を一連で自動実行する。

1. 指示された変更を実施する。
2. 変更内容に応じた必要なテストを行う。
3. 既存機能への影響を確認する。
4. `git status`で対象外ファイルが混ざっていないことを確認する。
5. 対象変更をcommitする。
6. `main`ブランチへpushする。
7. GitHub Pagesが対象commitを公開済みになるまで確認する。
8. 公開URLで変更内容が実際に反映されていることを確認する。
9. 公開ページのHTTP応答、画面エラー、主要機能を確認し、重大なエラーがないことを確認する。
10. 最後に`git status`を確認し、未commit変更がなく、`main`と`origin/main`が一致していることを確認する。

- `commitしますか？`、`pushしますか？`、`GitHubへ反映しますか？`などの確認は不要。
- commitには今回の対象ファイルだけを含め、既存の無関係な変更を勝手に含めたり破棄したりしない。
- GitHubリポジトリは `https://github.com/Piro-develop/vietnam-trip-2026`、公開URLは `https://piro-develop.github.io/vietnam-trip-2026/` を使用する。

## 自動公開しない指示

ユーザーが次のように明示した場合は、commit・push・GitHub Pages反映を行わない。

- ローカルだけで確認したい
- まだpushしないで
- GitHubには反映しないで
- 変更案だけ見せて

## 自動処理を停止する例外

次の場合だけ自動pushを停止し、必要な対応を一度に1つだけ具体的に案内する。

- パスワード、APIキー、アクセストークンなどの秘密情報を検出した場合
- 公開すべきでない個人情報が含まれる可能性がある場合
- GitHub認証エラーが発生した場合
- push競合が発生した場合
- GitHub Pages公開エラーが発生した場合
- 大量削除など、重大かつ元に戻しにくい変更になる場合
- この旅行アプリ以外のファイルまで変更する可能性がある場合

## 通常の完了報告

通常のアプリ修正完了時は、次の7項目だけを簡潔に報告する。

1. 変更内容
2. テスト結果
3. commit ID
4. push結果
5. GitHub Pages反映結果
6. 公開URL
7. 最終Git状態
